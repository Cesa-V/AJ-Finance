export function currencyToNumber(v){if(!v)return 0;return Number(String(v).replace(/\D/g,''))/100}export function formatCurrencyInput(v){return currencyToNumber(v).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}export function todayISO(){return new Date().toISOString().slice(0,10)}export function buildFaturas(n=8){const now=new Date();return Array.from({length:n},(_,i)=>new Date(now.getFullYear(),now.getMonth()+i,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'}))}export function splitTags(t){return String(t||'').split(',').map(x=>x.trim()).filter(Boolean).join(', ')}
